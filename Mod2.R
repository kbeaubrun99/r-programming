#Code from Mod 2 assignment
assignment2 <- c(16, 18, 14, 22, 27, 17, 19, 17, 17, 22, 20, 22)
myMean <- function(assignment2) { return(sum(assignment)/length(someData)) }
#This code won't run properly because it's missing input variables
#Will add assignment and someData variables so it will run properly
assignment <- c(17, 19, 15, 22, 27, 18, 20, 18, 18, 23, 21, 23)
someData <- c(17, 19, 15, 22, 27, 18, 20, 18, 18, 23, 21, 23)
#Now that we added the two vectors the function should execute correctly!
myMean(assignment2)